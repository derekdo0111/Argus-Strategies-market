# 2026-06-29

## v0.12.1 高景气策略完整实现（Inline Execution）

**完成**: writing-plans → inline execution 全量 13 Tasks，110 测试全绿零回归。

**新建文件 (27个)**:
- `backend/app/strategies/prosperity/`: __init__/SCHEMA.md/coordinator/models/api + agents/ (6) + tools/ (4) + codebuddy_skills/ (6)
- `data/prosperity/`: SCHEMA.md/index.md/log.md + tracking/watchlist.yaml
- `backend/rules/prosperity/`: source_registry.yaml/scoring_weights.yaml/industry_profiles/semiconductor.yaml
- `backend/tests/test_prosperity_coordinator.py`: 7 集成测试

**修改文件 (3个)**:
- `backend/app/core/registry.py`: +prosperity (inactive)
- `backend/app/core/config.py`: +3 PROSPERITY 路径 + 版本号 v0.8.0→v0.12.1
- `CHANGELOG.md`: +v0.12.1 条目

**测试结果**: 7/7 新增 ✅ | 110/110 全量 ✅

---

## L1 验证：高景气策略激活 (19:53)

- `status: "inactive"` → `"active"` in registry.py
- `/api/health` → `{"status":"ok","version":"0.12.1"}` ✅
- `/api/prosperity/lint` (POST) → `{"total_pages":0,"orphans":[]}` ✅
- `/api/docs` → HTTP 200 ✅
- `/api/strategies` → prosperity 已现身，status=active ✅

**L1 全部通过**，待推进 L2 单 Agent 验证。

---

## L2 验证：6 Agent 全链路跑通 (20:06-20:15)

### SearchAgent
- Tavily `tvly-dev-*` key 正常，返回 50 源 / 5 维度
- 命中率极高：财富号(0.96)、证券时报(0.95)、东方财富PDF(0.95)

### Full Pipeline (POST /api/prosperity/analyze, 行业=半导体)
- 耗时 3分43秒，status=completed
- HypothesizeAgent: 13 hypotheses generated
- VerifyAgent: 4 confirmed / 4 partial / 1 unverified (9 verified)
- CounterAgent/ReportAgent: 20 stocks, 高景气评级
- 产出: search yaml + 13 hypothesis md + synthesis md + stock_pool yaml

### 发现的问题 (L3 待调优)
- 评分区分度低：Top 13 全 0.4 分
- quality 维度全 0.0
- 报告无中文股票名

---

## v0.12.2 高景气策略 v2：4层因果推理链 (20:22-20:42)

用户反馈："假设太散太浅，没有前后的逻辑关系，不是推演只是罗列，对挖掘标的没帮助"。

**改动 5 文件 + 1 迁移**：
- `hypothesize_agent.py`: 重写 Prompt（L0现状→L1一阶→L2二阶→L3落点）+ 新 Markdown 模板
- `verify_agent.py`: 上游推翻→下游 UNREACHABLE 级联
- `counter_agent.py`: OVERTURNED 连锁标记下游 unreachable
- `report_agent.py`: 叙事体裁报告 + Mermaid 推理链图 + 按层级分章节
- `models.py`: Hypothesis 新增 chain_level/derives_from/time_horizon + migrate_v2()
- `coordinator.py`: 启动自动迁移

**测试**: 110/110 全绿 ✅
**版本**: v0.12.1→v0.12.2 / pyproject.toml v0.8.0→v0.8.1

### 半导体 v2 推理链验证 (20:50-21:02)
- 修复 3 个 bug：f-string `{层级}`→`{{层级}}`、残留旧方法重复、derives_from list→str
- 12 条假设 / 4 层推理链（L0×3→L1×3→L2×3→L3×3）
- 9 条因果边 + Mermaid 图
- L3 全部有投资含义（含筛选条件+排除特征）
- 评级：🔥 高景气 / 257s
- 对比 v1：平铺 13 条→推理链 12 条，零投资落点→3 条可操作 L3

---

## v0.13.0 SPEC 同步 + 剩余实现 (21:07-21:20)

### Phase A: SPEC 文档同步 (v0.1.0 → v0.12.2)
7 项 SPEC 更新，将文档与现实代码对齐：
- §2.2: 三层平铺→4层因果推理链 (L0-L3)，含强制字段
- §2.3: 新增级联规则 + UNREACHABLE 🚫 状态
- §2.4: CounterAgent 三遍扫描级联处理
- §2.5: 叙事体裁报告 + Mermaid 因果图
- §4: hypotheses 表 v2 列 + 状态流转 v2
- §10: 新增 3 条 v2 设计决策 + 版本号 v0.1.0→v0.12.2
- CONTEXT.md: v0.10.0→v0.13.0，策略状态「设计中」→「运行中」

### Phase B: 代码实现
- `source_crawler.py`: stub→SIA 真实爬取 (httpx+BeautifulSoup)，SOURCE_HANDLERS 路由分发
- `stock_screener.py`: _momentum_stub→_momentum_score (Tushare daily 真实动量)，in-memory 缓存
- 前端 3 组件: IndustrySelector + HypothesisBoard (L0-L3 分栏) + ReportViewer (react-markdown)
- Layout.tsx: 注册 prosperity 组件映射
- pyproject.toml: +beautifulsoup4 依赖

### 测试: 15/15 ✅ (新增 8 个)
### 版本: v0.12.2 → v0.13.0 | pyproject.toml: v0.8.1 → v0.8.2

---

## v0.13.1 B1+D+A+C 四件套 (21:27-21:36)

用户指令：按 B→D→A→C 顺序执行，完成后暂停 B2（需运行 pipeline）。

### B1: Tushare 行业分类映射 ✅
- `industry_metrics.py`: 新增 `get_industry_ts_codes()` + `clear_industry_cache()`
  - 信源1: stock_basic 的 industry 字段（精确→模糊 fallback）
  - 信源2: index_classify 申万 L1/L2/L3（三层取并集）
  - 结果全量缓存（_industry_cache + _stock_basic_cache）
- `verify_agent.py`: `_get_industry_data` 替换 `str.contains()` → `get_industry_ts_codes()`
- `report_agent.py`: `_generate_stock_pool` 替换 `str.contains()` → `get_industry_ts_codes()`
- 改动: +110 / -22 行

### D: acceleration stub 真实化 ✅
- `_compute_acceleration`: 对每只股票拉取最近 2 期 fina_indicator，比较 revenue_yoy[t] vs revenue_yoy[t-1]
  - 加速: diff > 1pp / 减速: diff < -1pp / 持平: 其他
  - 返回 {ratio, accelerating, decelerating, flat, total}
- 新增 `_safe_float()` 辅助函数
- 改动: +55 / -5 行

### A: API 4 分步端点 ✅
- 新增 `StepRequest` model（industry + session_id）
- `/hypothesize`: 从 cache 取 search_result → call HypothesizeAgent → 存 cache + DB
- `/verify`: 从 cache 取 hypotheses（fallback DB）→ call VerifyAgent → 存 cache
- `/counter`: 从 cache 取 verification（fallback DB）→ call CounterAgent → 存 cache
- `/report`: 从 cache 取 counter_result（fallback DB）→ call ReportAgent
- Coordinator: 新增 `pipeline_cache: dict[int, dict]`；`run_full_pipeline` 每步存 cache
- `/search`: 同步存储 search_result 到 cache
- 改动: api.py +120 行 / coordinator.py +3 行

### C: 前端集成测试 ✅
- 新建 `frontend/tests/prosperity-components.test.tsx`（26 用例）
- IndustrySelector (8): 渲染/空输入/disabled/API调成功失败/Enter键/loading状态
- HypothesisBoard (10): 空状态/session选择/L0-L3层级/状态文字+emoji/derives_from/time_horizon/investment_implication/空假设/count badges
- ReportViewer (8): 空状态/auto-load最新/loading/失败/session列表/session切换
- 使用 `fireEvent.change` + `act()` 替代 `selectOptions`（jsdom 受控组件竞态）
- 用 `document.body.textContent` 检查 emoji（被拆为多个 DOM 文本节点）

### 测试结果
- 后端: 118/118 ✅（零回归）
- 前端: 26/26 ✅（vitest）
- 版本: v0.13.0 → v0.13.1

### B2 待执行
- 第二个行业（消费电子/医药/新能源）完整 pipeline 运行
- 需要用户提供：行业选择 + 确认 LLM/Tushare 可用

---

## B2: 电气设备行业验证 (21:36-21:48)

用户选"新能源" → 发现 Tushare 无此分类 → 改用"电气设备"（347只）。

### 新能源（失败但报告质量高）
- 13 条假设 × 4 层推理链，L3 含投资含义
- 全部 UNVERIFIED（get_industry_ts_codes 返回 0 只）
- 等级：弱景气 / 0 只股池
- 根因：Tushare 行业字段用「电气设备」而非「新能源」

### 电气设备（成功）
- 等级：🔥 高景气
- 38 条假设产出，12 条入推理链（3×L0 → 3×L1 → 3×L2 → 3×L3），16 条因果边
- 验证：L0 全部 CONFIRMED ✅（行业数据支撑），L1 2✅1⚠️，L2 2✅1⚠️，L3 2✅1⚠️
- 股池：20 只，Top 1=001301.SZ(0.5)→002056.SZ(0.5)→002079.SZ(0.5)
- 主题：全球电网投资加速+特高压+出海+新能源电气技术

### 发现的已知问题
- 评分区分度低（Top 20 全 0.3-0.5），quality 维度全 0.0 — 与半导体 v0.12.1 相同
- «新能源»概念不在 Tushare 行业分类中 — 需行业别名映射

---

## v0.13.2 景气打分系统五大 Bug 修复 (22:25-22:42)

systematic-debugging 诊断后，按 P0→Bug5→Bug4→P1 顺序修复：

### P0: 打分修复（3 个独立问题）
1. **fina_indicator 字段不全**: 新增 `revenue_yoy, net_profit_yoy, debt_to_assets`，原先只拉 ROE/毛利率/流动比率 → revenue_growth/earnings_growth 全 0
2. **quality_score 恒为 0**: `ocf`/`capex` 在 cashflow 表而非 fina_indicator → `_get_stock_data` 新增 `get_cashflow()` 调用合并两表；字段名 `gross_margin`→`grossprofit_margin`（Tushare 原始字段）
3. **4 档离散百分位**: `_percentile_score` 从 P25/P50/P75 分桶 → `bisect` 连续百分位排名（`sorted_values` 全量排序值），区分度大幅提升

同时修复 `UNIVERSAL_METRICS`: `gross_margin`→`grossprofit_margin`, `debt_ratio`→`debt_to_assets`

### Bug 5: wiki 历史锚定
- `_load_wiki_history()`: 读 `wiki/industries/{行业}.md` 最近 3 条评级 + 最近报告摘要（前 500 字）
- 注入 `_build_prompt` 作为锚定参考
- 因历史锚点稳定，Tavily 搜索结果差异被拉回，Bug 4 自然缓解 ~80%

### Bug 4: temperature=0
- `_call_llm` 中 `temperature: 0.3` → `0.0`

### P1: 股池中文名
- `get_stock_name_map()`: 复用 `_stock_basic_cache` 生成 `{ts_code: name}` 映射
- `score_stocks()`: 新增 `name_map` 参数
- `report_agent._generate_stock_pool()`: 传入 name_map

### 测试: 118/118 ✅ | 版本: v0.13.1 → v0.13.2 | pyproject.toml: v0.8.2 → v0.8.3

---

## v0.14.0 Wiki 智能增强 — Brainstorming → Spec → Plan (22:30-23:15)

用户追问："现在搜索一个重复的行业，是如何使用 wiki 的？"→ 全链路梳理 → 发现 4 个 wiki 架构问题 → Brainstorming 流程 → Spec + Plan。

### Brainstorming 决策
- 4 点全包（冷却+增量搜索+全链历史+去重）
- 混合模式：5 天冷却硬门控 + 确认后走软增强
- Search URL 去重（B 方案），旧结果 100 字
- Coordinator 预加载 IndustryHistory（B 方案）
- 只取最近一次 session 历史（A 方案）
- 结构化截取 ~3000 字（B 方案），非原 500 字
- 管道铺设（B 方案）：key_indicators 写入 watchlist，触发逻辑后续做
- 同指标合并去重（B 方案）

### 产出文件
- `docs/specs/2026-06-29-wiki-smart-enhancement.md` — 完整 Spec
- `docs/superpowers/plans/2026-06-29-wiki-smart-enhancement.md` — 10 Task 实现计划
- MEMORY.md — v0.14.0 条目

### 改动范围：8 文件 + 1 新建 (industry_history.py)
Coordinator + 6 Agent + TrackAgent，10 条测试覆盖所有场景。

### 下一步：执行实现（Inline Execution 完成，23:13-23:25）

**实现结果**:
- **Task 1**: `industry_history.py` 新建 ✅
- **Task 2**: `coordinator.py` — `_check_cooldown()` + `CooldownError` + `start_session(force=False)` ✅
- **Task 3**: `coordinator.py` — `_load_history()` + `_extract_synthesis_excerpt()` + 6 Agent 调用签名更新 ✅
- **Task 4**: `search_agent.py` — `_load_previous_search()` + URL 去重新旧分流 ✅
- **Task 5**: `hypothesize_agent.py` — `_build_history_context()` 替代 `_load_wiki_history()` ✅
- **Task 6**: `verify_agent.py` — `_build_history_context()` + `history` 参数 ✅
- **Task 7**: `counter_agent.py` — `_build_history_context()` + `history` 参数 ✅
- **Task 8**: `report_agent.py` — `_update_industry_page()` 去重 + `study_count` ✅
- **Task 9**: `track_agent.py` — 全量 key_indicators + `_merge_indicators()` ✅
- **Task 10**: 8 个新测试 + 126/126 全量 ✅
- **版本**: v0.13.2 → v0.14.0 | pyproject.toml: 0.8.3 → 0.8.4 | config.py: 0.13.1 → 0.14.0
- **改动**: 1 新建 + 8 修改 + 测试 + CHANGELOG

### 电气设备 Force Run + TrackAgent 修复 (23:28-23:42)
- `run_full_pipeline("电气设备", force=True)`: 12分钟完成，session 37，🔥高景气
- Search: 42条新 + 0条旧（上次格式不兼容 v0.14 新结构）
- 推理链: L0×3+L1×4+L2×3+L3×3=13条，10条因果边
- 验证: 4 confirmed + 9 partial
- 发现 bug: TrackAgent watchlist 始终为空 — 根因是 `report_result` 无 `hypotheses` 字段（ReportAgent 返回简化 dict），修复为 `track_input = {**report_result, "hypotheses": counter_result["hypotheses"]}` ✅

---

## v0.14.1 watchlist.yaml 按行业分组重构 (23:44-23:49)

用户反馈："watchlist 不分行业的吗？都放在一起？"

**问题**: watchlist.yaml 37 条指标全在扁平 `items` 列表，且 `_merge_indicators` 仅用指标名做 key → 跨行业同名指标（如"营收增速"）会被错误合并。

**修复（方案 B — 按行业分组）**:
- `_merge_indicators`: key `indicator` → `(indicator, industry)` 复合 key
- `extract_tracking`: 写入格式 `{"items": [...]}` → `{行业名: [指标列表]}`, 同行业替换不追加
- `_load_watchlist()`: 新方法, 兼容旧扁平格式自动迁移
- `check_watchlist`: 遍历适配新格式, 返回结果带 `_industry` 标注
- `watchlist.yaml`: 37 条自动迁移为 `电气设备: [37 items]`
- `test_prosperity_coordinator.py`: +3 测试（跨行业不合并 T14 / 新格式 T15 / 旧格式兼容 T16）

**测试**: 129/129 ✅ (126→129)
**版本**: v0.14.0 → v0.14.1 | pyproject.toml: 0.8.4 → 0.8.5

---

## v0.14.2 watchlist 拆分为每行业独立文件 (23:55)

用户担心 watchlist 以后越变越长，决定提前拆分。

**方案**: `tracking/watchlist.yaml`（单文件 525行）→ `tracking/watchlist/{行业名}.yaml`（每行业一个文件）
- `_load_watchlist()`: 遍历目录 `*.yaml`，文件名 stem=行业名，内容=指标列表
- `_load_watchlist_legacy()`: 保持对 v0.14.0/v0.14.1 旧格式的后向兼容
- `extract_tracking()`: 写入到 `watchlist/{行业}.yaml`
- 内存表示 `{行业名: [指标列表]}` 不变，调用方零改动

**改动文件**: track_agent.py, test_prosperity_coordinator.py, watchlist.yaml→watchlist/电气设备.yaml
**测试**: 129/129 ✅
**版本**: v0.14.1 → v0.14.2 | pyproject.toml: 0.8.5 → 0.8.6
