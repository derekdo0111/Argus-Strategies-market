# 2026-06-26

## v0.10.0 — 高景气假说管理闭环 Phase 1 完整落地

### 已完成
- **B 层多源交叉验证器** (`claim_verifier_b.py`): Tavily 搜索 ≥3 来源 → 正则提取数值 → 中位数判定。支持 11 种字段：渗透率、市占率、CR3、ASP、CAGR 等
- **C 层 LLM 证据链验证器** (`claim_verifier_c.py`): Tavily + LLM → 假说成立/不成立。10 条 C 层假说全覆盖
- **LLM 纠正引擎** (`hypothesis_corrector.py`): disputed → LLM → refine/retire/keep
- **迭代控制器** (`iteration_controller.py`): verify → disputed → correct → re-verify → converge，默认 5 轮
- **持久化层** (`memory_store.py`): YAML 三层分离（verification/*-latest.yaml 覆盖 + corrections/*.yaml 追加 + memory/iterations/*.yaml 追加）
- **verify.py 重写**: `--layer B/C` 支持 + `--correct` 迭代纠正模式 + `--max-iter`
- **config.py**: 新增 `PROSPERITY_MAX_ITERATIONS=5`
- **测试**: 202/202 全部通过 (180 已有 + 22 新增)

### 新增文件 (8)
- `claim_verifier_b.py`, `claim_verifier_c.py`, `hypothesis_corrector.py`, `iteration_controller.py`, `memory_store.py`
- `tests/test_claim_verifier_b.py`, `tests/test_claim_verifier_c.py`, `tests/test_memory_and_corrector.py`

### 修改文件 (5)
- `verify.py`, `__init__.py`, `config.py`, `.env`, `pyproject.toml`

### 关键设计决策
1. **模板可修改**: LLM 纠正引擎更新 stated_value + change_log，不删历史
2. **Schema 不可改**: claim-schema.yaml v1.0 宪法不动
3. **三层并行**: A/B/C 验证同时进行（不同数据类型）
4. **D 层保留**: 预测型假说不参与当前验证循环，留到后续版本

### 下一步
- Phase 2: 长期记忆查询 + 景气度排名股池
- B 层需升级为 LLM 辅助提取（正则只能命中部分字段）

## v0.10.1 — 取消单层模式，统一闭环入口 (14:05)

- **verify.py** 简化: 移除 `--layer` / `--correct` 参数，统一为迭代纠正闭环
  - 删除 6 个函数: `format_result()`, `run_single_layer()`, `run_layer_a/b/c()`, `print_summary()`, `run_correct_loop()`
  - 逻辑内联到 `main()`，CLI 参数精简为 `--track`, `--max-iter`, `--full`, `--verbose`, `--list`
- **pyproject.toml**: v0.10.0 → v0.10.1
- **CHANGELOG.md**: 新增 v0.10.1 条目
- **202/202 pytest** ✅ 全部通过

## v0.10.0 全流程跑通结果 (12:26)

`--correct --max-iter 1` 首次全闭环验证结果 (35 条):

```
A 层: 14条 →  6 verified /  7 disputed / 0 skipped /  1 error
B 层: 11条 →  0 verified /  1 disputed / 7 skipped /  3 error
C 层: 10条 →  2 verified /  3 disputed / 0 skipped /  5 unclear
──────────────────────────────────────────────────────────
总计: 35条 →  8 verified / 11 disputed / 7 skipped /  9 error/unclear
LLM 纠正 11 条 disputed → 验证快照 + 纠正审计已落盘
```

C 层效果亮眼: L4-Q003/Q004/Q007/Q008/Q009/Q010 全部 verified/部分 verified。L4-Q005 (纯视觉支撑L4) 被明确 disputed。

### Bug 修复
1. **GBK 编码**: 所有 print 中的 emoji → ASCII 替代
2. **YAML 模板更新 Bug**: `_update_template` 的 `---` 分隔符 + Windows `\r\n` 导致重解析崩溃
   - 修复: 改为写独立 `verification/{track}-state.yaml` 状态文件，不动 Markdown 模板
   - 模板作为"原始假设"永久保留，运行时状态存 YAML 状态文件
3. **模板损坏恢复**: 第一次错误更新导致 SZ-001 status 被改成 refined + `---#` 拼接，已手动恢复

## v0.10.2 — 核心引擎 P0 Bug 修复 (19:46)

### 修复内容
- **Bug #2 迭代永不收敛** (最严重): `run()` 中重解析模板后调用新增的 `_load_state(claims)` 从 `-state.yaml` 加载纠正后的 stated_value/status，覆盖模板原始值
- **Bug #6 状态机静默吞错**: `_apply_correction` 和 C 层纠正中 `try/except: pass` → `logger.warning`；自动补全状态路径 `proposed → under_review → disputed → refined`
- **Bug #1 北向资金纠正死循环**: `NorthboundSource` 无法归一化时返回 `value=None` (`skipped`) 而非万元绝对值，避免与百分比阈值单位不匹配产生 21,600,096% 的无意义偏差

### 文件改动
- `iteration_controller.py`: +80 行 (`_load_state` 方法 + 修复 `_apply_correction` / `_run_corrections`)
- `data_sources.py`: NorthboundSource 兜底逻辑 `value=total_flow` → `value=None, skipped_reason`
- `CHANGELOG.md`, `pyproject.toml`: v0.10.1 → v0.10.2

### 测试
- **202/202 pytest** ✅ 全部通过，零回归
