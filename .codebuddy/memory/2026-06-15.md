# 2026-06-15

## 完成工作：数据质量修复 + 三级缓存 + SPEC 合规测试

### 一、Bug 修复
1. **选股器条件10删除** (`screener.py`): Tushare `ocf_to_or` = OCF/Revenue ≠ OCF/NetProfit，该字段含义错误导致候选池从预期的 300-800 缩到 49。删除后由 CQ维度1 精确检查替代
2. **Coordinator 终止逻辑** (`coordinator.py`): 拉取成功率 < 90% 现在真正终止并返回 `[]`（原只log不阻止）
3. **candidate_pool.yaml 输出** (`coordinator.py`): 新增 `_write_candidate_pool()` 方法，满足 SPEC Step1 L122
4. **计算前校验** (`coordinator.py`): 新增 `_validate_raw_data()` 检查财务年数、必须字段、total_mv，不合格跳过计算（满足 ADR-0003 步骤间校验要求）
5. **候选池告警可视化**: `logger.warning` → `print` 确保终端可见

### 二、三级缓存优化
| 级别 | 文件 | 作用 | 方式 |
|------|------|------|------|
| L0 | `_stock_basic_cache.parquet` | Step0 全A股基础信息 | 同日缓存复用 |
| L1 | `candidate_pool.yaml` | Step1 选股结果 | 始终写入 |
| L2 | `raw_data.yaml` | Step2 个股全量数据 | 跳过已有完整数据 |

新增 CLI 用法：
- `--compute-only` : 使用所有缓存，只跑计算 (~30s vs 10min)
- `--force` : 强制忽略所有缓存重拉

### 三、SPEC 合规测试
- 新建 `backend/tests/test_spec_compliance.py` — 15 个测试
- 覆盖: screener 条件数 / coordinator 校验 / candidate_pool 输出 / fetch 终止 / L0缓存
- 安装 `pytest-asyncio` 支持 async 测试
- 更新旧测试 `test_opcf_too_low` → `test_opcf_not_screened`
- 全部 46 个测试通过 (31 单元 + 15 合规)

### 改动文件
- `backend/app/strategies/turtle/screener.py` — 删除条件10 + opcf_to_netprofit 字段
- `backend/app/strategies/turtle/coordinator.py` — 终止+校验+输出+可见告警
- `backend/app/services/data_fetcher.py` — L0+L2缓存 + 移除错误字段映射
- `scripts/run_turtle_refresh.py` — --compute-only / --force
- `backend/app/strategies/turtle/turtle-coordinator.md` — SPEC更新
- `backend/tests/test_spec_compliance.py` — 新建
- `backend/tests/test_screener.py` — 更新旧测试

### 四、候选池阈值收紧 (20:04)
- 全量刷新结果 970只太多，目标控制到 ~100只
- 7项阈值调整:
  | 条件 | 旧值 | 新值 |
  |------|------|------|
  | 上市年限 | > 5年 | > 8年 |
  | 市值 | > 50亿 | > 200亿 |
  | ROE | > 0% | > 12% |
  | PE | 0-50 | 5-25 |
  | 股息率 | > 0% | > 2.5% |
  | 毛利率 | > 10% | > 25% |
  | 负债率 | < 80% | < 60% |
- 同步更新: `screener.py` 硬编码阈值, `rules/v1/turtle_screener.yaml`, `turtle-coordinator.md` SPEC, `coordinator.py` 告警阈值(200→50)
- 新增边界测试: ROE<12, 股息<2.5, 上市<8, 市值=200边界
- 测试从 46→49 个，全部通过

### 五、股池 0 问题修复 + 文件夹改名 (20:19)
- **根因**: PR阈值 5.5%(2.5%+3.0%) 太高，所有股票PR在 2.5-3.0% 范围，全淘汰
- **修复**: `.env` TURTLE_RISK_FREE_RATE 2.5→1.7, TURTLE_SPREAD 3.0→1.0 (阈值降至2.7%)
- **关键教训**: `.env` 文件覆写代码默认值，改代码不生效时必须检查 `.env`
- **PR计算修复**: 分红/回购按财年聚合求和 (同一财年多次分红之前只取3条而非求和)
- **文件夹改名**: `{ts_code}` → `{名称}_{ts_code}` (如 `贵州茅台_600519.SH`)，新建改名脚本 `scripts/rename_folders.py`
- **coordinator 兼容**: 读取时先查新路径再回退旧路径，写入用新路径
- **最终结果**: 5529 → 选股39 → CQ通过26 → PR通过**10只** (PR 2.88-4.63%)
- 股池: 海澜之家/苏泊尔/亿联网络/东阿阿胶/伊利/云南白药/华润三九/迈瑞医疗/吉比特/泸州老窖
