# 2026-07-08 工作日志

## v0.23.2 纯度分全 0 Bug 修复
- **根因**: `investment_implication` 字段是 dict（`{'受益环节': '...', '典型标的特征': '...'}`），`_extract_l3_keywords()` 中 `re.split(dict)` → TypeError → 被 `_compute_purity()` 的 `except Exception` 静默吞掉 → 全返回 0
- **修复**:
  1. `purity_scorer.py` 新增 `_flatten_field()` — dict 时拼接 values 为字符串
  2. `_extract_l3_keywords()` 三字段均通过 `_flatten_field()` 转换
  3. `_llm_match_fallback()` 同理处理
  4. `screening_agent.py` 新增 `_flatten_implication()` 本地版本
  5. `_llm_direction_match()` 同理处理
  6. `_compute_purity()` 异常处理加 `traceback.format_exc()` 日志
- **测试**: 204/204 ✅
- **版本**: v0.23.1 → v0.23.2 | pyproject: 0.23.1 → 0.23.2

## 用户答疑：代码安全网 + 完整流程 + 各状态
- 用 Session 85 人工智能实例详细讲解了安全网机制（两次触发：H2-3/H3-3 unverified 被 LLM 误判 overturn → 代码拦截改写为 downgrade_confidence）
- 6 种状态逐一举例（confirmed/partial/unverified/disputed/overturned/unreachable），含精确的代码引用行号

## v0.23.3 概念板块构建器升级 Bocha 主搜索
- **背景**: 用户问"为什么兆易创新这种存储龙头没被纳入股池" → 排查发现概念板块构建器(c concept_builder)仍硬编码用 Tavily，未跟随 v0.23 SearchAgent 的 Bocha 升级
- **根因**: 兆易创新在 Tavily 搜索结果中存在，但 LLM 提取窗口仅 8000 字符可能被截断；同时 H3-1(算力硬件) 被 CounterAgent 推翻 → 即使进池也会低分
- **修复**: concept_builder.py 新增 `_detect_engine()` + `_bocha_search()` (对齐 SearchAgent)，Bocha 失败自动降级 Tavily
- **副作用**: 删除旧缓存 `人工智能.yaml`
- **测试**: 204/204 ✅ | 版本: v0.23.2 → v0.23.3

## v0.23.4 VerifyAgent + TrackAgent 全部升级 Bocha 主搜索
- **背景**: 用户反馈 "前面也还是报错Tavily" — verify_agent 和 track_agent 仍硬编码直接调 Tavily
- **修复**:
  1. `verify_agent.py`: 新增 `_detect_engine()` + `_bocha_search()` + `_tavily_counter_search()`（降级），`_execute_counter_searches()` 引擎分发
  2. `track_agent.py`: 新增 `_detect_engine()` + `_web_search()` + `_bocha_search()`，`_tavily_search()` 降级为备用，`_check_single()` 改用 `_web_search()`
- **架构**: 至此 Prosperity 全部 4 个 Agent + 1 个 Tool 完成 Bocha 主搜架构
- **测试**: 204/204 ✅ | 版本: v0.23.3 → v0.23.4

## v0.23.5 ConceptIndex 概念板块本地索引
- **用户确认**: ConceptIndex 不放在 wiki 里维护，是独立的基础设施组件
- **新增**: `tools/concept_index.py` — 全量拉取 409 个概念板块 + rapidfuzz 模糊搜索
  - `build()`: ths_index(type=N) → data/concept_index.yaml (24h TTL)
  - `search()`: rapidfuzz WRatio，"新能源"→"新能源汽车" 90%
  - `resolve()`: 搜索+ths_member 一键返回成分股
- **集成**: `industry_metrics.py` get_industry_ts_codes() 新增信源0 ConceptIndex 优先
- **依赖**: +rapidfuzz ^3.14.0 (pip install)
- **测试**: 204/204 ✅ | 版本: v0.23.4 → v0.23.5

## v0.23.6 成分股超限交互式子板块推荐
- **背景**: 用户发现"人工智能"111只成分股被 `[:50]` 硬截断丢了61只 → 讨论不能用财务预筛（误杀景气早期标的）→ 确定交互式子板块推荐方案
- **实现**:
  1. `concept_index.py`: 新增 `suggest_subconcepts(main_name)` — rapidfuzz 搜索 + count过滤 + 排除自身
  2. `config.py`: 新增 `PROSPERITY_SCREENING_THRESHOLD: int = 50`
  3. `coordinator.py`: 新增 `check_industry_size(industry_name)` — 预检+推荐列表
  4. `screening_agent.py`: 删除 `[:50]` 硬截断
  5. `run_prosperity_ai.py`: 重构为完整 CLI（argparse + 交互菜单 + --force-full）
- **版本**: v0.23.5 → v0.23.6

## v0.23.7 resolve() 精确命中修复
- **根因**: `resolve("人工智能")` 对 3 个匹配概念（人工智能 885728.TI、AI智能体 886099.TI、AIGC概念 886019.TI）求并集 → 1074 只，而非精确命中时的 1060 只
- **修复**: `concept_index.py` `resolve()` 改为精确命中（score=100）只用那一个概念，模糊匹配只用最佳匹配，不再求并集
- **测试**: 204/204 ✅ | 版本: v0.23.6 → v0.23.7

## 存储芯片景气分析（Session 111）
- **运行**: `python run_prosperity_ai.py 存储芯片 --force --force-full`，201只成分股全量，耗时 19.2min
- **结果**: 评级 **景气**，信号强度 1.53
- **推理链**: L0 AI算力拉动+供需失衡+厂商扩产 → L1 涨价(被推翻)/HBM倾斜/设备需求 → L2 原厂利润率(不可达)/消费级供给收缩(被推翻)/设备订单爆发/国产替代
- **CounterAgent**: overturned=2 (H1-1涨价, H2-2消费级供给收缩), unreachable=3 (H2-1/H3-1/H3-3)
- **股池**: 167只入选，Top: 香农芯创 73.0%、雅创电子 72.5%、诚邦股份 67.3%。兆易创新排 #46 (40.53%, ROE 6.6%, 毛利率 57.1%)
- **问题**: LLM方向匹配超时 120s，导致所有股方向分均为 0.50（中性），方向维度未发挥作用。几乎所有股都匹配到 H3-2（国产设备与材料），投资落点过于集中
- **AI全产业链池讨论**: 用户想多池交叉筛出真AI核心标的。202只成分股仍可控，但方向匹配超时是需要关注的问题

