# 2026-07-10

## v1.2.0 ScreeningAgent 重构：精选推荐
- **动机**: v1.1.0 LLM 一次性 201 只超时 → 程序化兜底无假设命中 → 50 只纯财务排名
- **新架构**: 程序化预筛+分段+全量财务预排 → LLM 分段精选(每段挑 6 只) → 打分排名
- **关键设计**: 代表公司强制豁免候选池（解决财务排名排除战略重要股）；purity_estimate 程序化营收比例计算；下游排他关键词（模组/SSD 优先归 downstream）
- **config**: `PROSPERITY_SCREENING_TOP_PER_SEGMENT=6` 替代旧 `BATCH_SIZE`
- **测试**: 208/208 ✅
- **改动文件**: screening_agent.py(重写), screening_direction_prompt.md(重写), screening_scorer.py(+selection_reason), report_agent.py(+挑选理由列), config.py, pyproject.toml(1.1.0→1.2.0), test_prosperity_coordinator.py(2测试适配), CHANGELOG.md

## v1.2.1 VerifyAgent 冲突阈值 + ReportAgent 级联修正展示

- **Bug 1 修复**: `_aggregate_rounds()` 弱冲突(score=2)多轮一致→strong 错判为 overturned。旧规则 `≥2轮≥2分→strong` 太过激进（H0-2 3轮全打2分即被推翻）。新规则: 仅 `≥1轮=3分 + ≥2轮有冲突` → strong。3轮全2分 → weak → weak_disputed（不切链）
- **Bug 2 修复**: `_render_report()` 不展示 CounterAgent 修正信息。新增: `original_sentiment` 变更展示 + `causality_note` 中 CounterAgent 级联裁决原因提取
- **改动文件**: verify_agent.py(L851-859), report_agent.py(L217-233→L217-257), pyproject.toml(1.2.0→1.2.1), CHANGELOG.md, MEMORY.md
- **测试**: 208/208 ✅
- **结果**: 评级「景气」, 信号强度 2.52, 耗时 987s (16.4min)
- **假设**: 12条 → 确认2(AI驱动超级景气/国产化扩产加速), 部分5, 推翻1(HBM短缺), 不可达2(全品类涨价/模组厂红利因上游推翻)
- **股池**: 16只 (上游设备6/中游设计6/下游模组4)
- **CounterAgent安全网**: 拒绝推翻 H1-1/H1-3/H2-1/H2-3/H3-1/H3-3 (正常行为)
- **已知问题**: concept_detail API 失败(存储芯片非Tushare概念索引), suggest_subconcepts 乱码(编码问题, 被 --force-full 兜底)

## v1.2.1 第二次运行 存储芯片（Session 178）
- **时间**: 2026-07-10 12:59, 耗时 1166s (19.4min)
- **评级**: 高景气
- **假设**: 12条 → confirmed=3 partial=3 unverified=6, CounterAgent overturned=0 unreachable=0
- **股池**: 18只 (上游6/中游6/下游6) — 比上次16只多2只，下游从4只补齐到6只
- **TOP3**: 江波龙(1.187) / 中微公司(0.914) / 拓荆科技(0.525)
- **变化**: 江波龙 composite 从 0.651→1.187 跃升，LLM 多命中 5 条假设(vs 上次3条)
- 201 只成分股 → 161 只预筛 → 18 只精选

## Git Push v0.15.0 → GitHub
- **commit**: 8de00d5 `feat: v0.15.0 高景气策略全链路 + 前端景气报告面板 + Wiki-Centric 知识复用`
- **253 files**, +62,536/-2,424 行
- **安全检查**: `.env` 已在 `.gitignore` (第24行)，确认未泄漏；内嵌仓库 `.codebuddy/skills/prompt-framework-optimizer/` 已从暂存区移除并加入 gitignore
- **冲突**: 仅 README.md，已解决（合并远程 v0.16.0 + 本地高景气策略说明）
- **Rebase**: 成功基于 `eb95794` (Update README.md)
- **新增 gitignore 规则**: `*.log`, `*_output.txt`, `.pipeline_output.txt`, `backend/outputs/`, `backend/data/`, `.codebuddy/settings.local.json`
- **前端 mockups**: 3版景气报告 UI 设计稿 (v1三栏/v2双栏/v3沉浸阅读)已入库

## v2.0 高景气策略脑图UI全面改版

基于用户反馈的8个问题全面重写 `prosperity-mindmap.html`（46KB）：
1. **侧边栏Toggle修复**: 点击"高景气策略"溃缩后再点弹出（之前只溃缩不弹出）
2. **可拖拽面板**: 4个面板之间各加 resize-handle（5px），mousedown拖拽调整宽度，localStorage持久化
3. **信号强度**: 数字4.26 → 🔥 + "高景气"文字，不用数字
4. **脑图推演过程**: 卡片上方显示依赖提示（↑ H0-1, H0-2），connector zone显示流程标签（事实→推演、推演→矛盾、矛盾→策略）
5. **卡片内容重构**: 展开后分4段 — 📌假设陈述（蓝色左边框高亮）、🔗推理链（→ 箭头可视化因果）、🔬验证诊断（因果链强度badge+证据）、📊跟踪指标
6. **股池分数**: 显示为"景气适配分"（composite score），去掉不明含义的数字
7. **雷达图简化**: 6轴（景气纯度/基本面质量/抗风险力/盈利能力/毛利水平/成长速度）全部归一化0-100，去掉"八角能力映射"标签，增加数据面板
8. **知识图谱**: 顶部新增行业知识图谱卡片（8组核心数据 + 8个高亮标签芯片）

生成方式: data.json（数据） + css.txt（样式） → gen_html.py → prosperity-mindmap.html，生成后清理临时文件

## v2.0.1 高景气脑图三处修复
1. **侧边栏对齐龟龟**: 默认collapsed(56px)，hover展开(260px)，mouseenter/mouseleave事件驱动，与龟龟Layout.tsx行为一致。去掉之前的toggle逻辑（再点不弹回）。
2. **知识图谱景气亮点**: kg-section内新增 kg-spotlight区域（暖橙渐变背景+边框），显示5个当前最景气点：HBM缺口4-5%、AI存储年增60%+、DDR5渗透>50%、两长扩产提速、HBM4 2026量产。
3. **雷达图八角→六角**: SVG网格4层六边形(6顶点)替换原8顶点多边形，3条轴线(垂直/-30°~150°/30°~210°)替换原4条，6个轴标签修正为：景气适配/基本面质量/抗风险力/ROE/毛利率/营收增速，与selectStock()的6轴数据映射一致。

## v2.0.2 雷达图"抗风险力"→"扣非净利润增速"
- **原因**: risk_exposure 真实数据 14/18 只=0，区分度为零（上游+中游命中负面假设数为0，仅4只下游模组有0.001~0.147）。存储芯片超级景气周期下负面假设全被推翻，该维度无实际用途。
- **替换**: 雷达轴3(30°) 抗风险力→扣非净利润增速，归一化范围 (-400, 800)，18只股票 net_profit_yoy 范围 -354.3%~7835.1%，区分度拉满。
- **改动**: selectStock() 签名加 deducted 参数（位置在 revenue 后 risk 前），18处调用全补真实 net_profit_yoy 值，SVG标签+默认指标面板同步更新。

## v2.0.3 侧边栏拖拽后溃缩失效修复
- **根因**: 拖拽 resize 写 `style.width` 内联样式 → 优先级高于 CSS `.sidebar-left.collapsed` → `classList.toggle('collapsed')` 失效
- **修复**: `switchStrategy` 改用内联样式直写宽度（收起=56px, 展开=sidebarExpandedW），新增 `sidebarExpandedW` 变量（默认260px，mousemove 中实时追踪侧边栏拖拽宽度）

## v1.2.2 高景气策略 Mockup → React 前端合并 (2026-07-10 19:45)
- **动机**: `prosperity-mindmap.html` (46KB单文件) 与 React 主应用完全割裂，前端切换「高景气策略」只显示简陋假设列表
- **新建文件**: ProsperityMindmap.tsx/m.module.css (摘要+KG+L0-L3推理链 12卡), ProsperityStockPanel.tsx/m.module.css (上中下游Tab+18只股票+SVG六边雷达), prosperity-types.ts, prosperity-data.ts (Mock数据)
- **修改文件**: Layout.tsx (component映射替换), index.css (加--l0/--l1/--l2/--l3层级色变量)
- **数据状态**: 目前硬编码存储芯片Mock数据（18只股票+12条假设+KG），后续接后端API
- **编译**: tsc --noEmit 通过，仅旧文件HypothesisBoard.tsx/ReportViewer.tsx的预存unused-variable警告
- **联动**: 左栏侧边栏→中栏脑图→右栏股票面板+雷达图，三栏联动，色系统一
