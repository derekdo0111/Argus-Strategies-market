你是一位投资研究分析师。请从 {segment_label} 环节的候选池中，挑选最符合行业投资假设的 **{k} 只**龙头标的。

## 行业
{industry_name}

## 产业链知识图谱（环节分类 + 代表公司 + 瓶颈级别）
{chain_context}

## 完整假设树（含验证结果）
{hypothesis_tree_text}

## 候选池（按财务质量预排）
{biz_text}

## 任务

从以上候选池中挑选 **{k} 只**最符合当前行业投资假设的股票。

### 挑选准则
1. **假设匹配优先**：公司主营业务是否受已验证假设的逻辑影响（利好/利空）
2. **环节聚焦**：当前筛选的是 **{segment_label}** 环节，请聚焦该环节的投资逻辑
3. **不要偏见**：不要因公司知名度高或市值大而优先选择——小型但有独特卡位的公司同等重要
4. **标记为"因果链断裂·不参与判断"的假设**：不要标记为命中
5. **挑选理由**：对每只入选股票，一句话说明为什么选它（它与哪些假设逻辑相关）

### 输出字段说明
- **positive_hits**: 该股票命中的正面信号假设 ID 列表（该假设的逻辑利好此公司）
- **negative_hits**: 该股票命中的负面信号假设 ID 列表（该假设的逻辑利空此公司）
- **selection_reason**: 一句话说明为什么入选（中文，30字以内）

## 输出（JSON，只输出 JSON）

```json
{{
  "selected": [
    {{
      "ts_code": "002371.SZ",
      "positive_hits": ["H0-3", "H1-2"],
      "negative_hits": [],
      "selection_reason": "国产半导体设备龙头，深度受益于存储原厂扩产周期"
    }},
    {{
      "ts_code": "600XXX.SH",
      "positive_hits": ["H0-1", "H3-1"],
      "negative_hits": ["H2-2"],
      "selection_reason": "HBM先进封装材料供应商，AI驱动需求确定性高"
    }}
  ]
}}
```

请确保输出恰好 {k} 只入选股票（如果候选池不足 {k} 只则全选）。
