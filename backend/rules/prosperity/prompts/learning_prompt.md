你是一位行业研究分析师。请基于以下搜索素材，为「{industry_name}」构建产业图谱。

## 搜索素材
{search_results}

## 输出要求

按以下 8 个章节输出。§1-7 使用 Markdown 格式，§8 使用 YAML 代码块。严格遵循结构，不要额外内容。

### 1. 价值链
按上游→中游→下游梳理产业链。每个环节描述：
- 角色（这个环节做什么，在整个产业链中的位置）
- 上市公司及代码（如西部超导 688122.SH，只列出搜索素材中出现的上市公司）
- 当前状态（基于搜索素材判断，引用信源编号）
- 引用信源编号（如 [信源1,5]）

### 2. 供需格局
使用表格，分别列出需求端和供给端：

**需求端表格**：
| 驱动力 | 类型 | 确定性 | 时间窗口 | 信源 |
|--------|------|--------|---------|------|
| ... | 政策/资本/出口等 | 高/中/低 | 2025-2030 | [N] |

**供给端表格**：
| 环节 | 当前供给状态 | 扩产周期 | 关键约束 | 信源 |
|------|------------|---------|---------|------|
| ... | 偏紧/充裕/过剩 | 1-3年 | ... | [N] |

末尾用一段话给出「供需判断」：当前供需状态 → 未来趋势 → 主要风险。

### 3. 技术路径与成熟度
使用表格列出所有技术路径：

| 路径名称 | 成熟度 | 代表项目/公司 | 优势 | 风险 | 信源 |
|---------|--------|-------------|------|------|------|
| ... | 科学验证/工程验证/商业示范/规模化 | ... | ... | ... | [N] |

### 4. 政策催化
使用表格列出关键政策或事件：

| 政策/事件 | 时间 | 影响环节 | 力度评估 | 信源 |
|-----------|------|---------|---------|------|
| ... | 2025-2030 | 全链条/上游/中游/下游 | 强/中/弱 | [N] |

### 5. 关键瓶颈
使用表格列出产业层面的卡脖子点：

| 瓶颈描述 | 严重程度 | 影响环节 | 证据 |
|---------|---------|---------|------|
| ... | 高/中/低 | ... | [N] |

### 6. 跟踪指标
使用表格列出可用公开数据跟踪的指标：

| 指标名称 | 巡检频率 | 含义 |
|---------|---------|------|
| ... | daily/weekly/monthly/quarterly | 这个指标变化代表什么 |

### 7. ⚠️ 不确定区域
用自然语言（不要用表格）列出：基于当前搜索素材，哪些关键问题无法回答、存在分歧、或需要进一步验证。

### 8. YAML 结构化数据
基于以上 7 节的内容，输出一份 YAML 格式的产业链结构化数据。YAML 必须放在 ` ```yaml ` 代码块中，结构如下：

```yaml
industry: {industry_name}
chain:
  segments:
    - id: upstream_xxx          # 英文ID，用下划线
      name: 上游XXX             # 中文名
      position: upstream        # upstream / mid / downstream
      description: ...
      bottleneck:
        level: low              # low / medium / high / critical
        detail: ...             # 瓶颈描述
        localization_rate: 80   # 国产化率粗估 %（整数）
      representative_companies:
        - 公司名
    - id: mid_xxx
      name: 中游XXX
      position: mid
      ...

bottlenecks:
  - segment_id: upstream_xxx
    severity: medium            # low / medium / high / critical
    description: ...

supply_demand:
  overall_judgment: 严重供需短缺    # 或 供需平衡 / 供给过剩 / ...
  demand_drivers:
    - driver: AI需求爆发
      certainty: high           # low / medium / high
      window: 2026-2028
  supply_constraints:
    - constraint: HBM产能偏紧
      detail: ...

technology_paths:
  - name: HBM
    maturity: 规模化            # 科学验证/工程验证/商业示范/规模化
    representative: SK海力士
  - name: DDR5
    maturity: 规模化

tracking_indicators:
  - name: DRAM合约价
    frequency: monthly          # daily / weekly / monthly / quarterly
    meaning: 反映供需紧张程度
```

注意：YAML 中不要编造 Markdown 中没有的信息。如果某节在 Markdown 中信息不足，对应 YAML 字段留空或省略。

## 核心规则

1. **每条事实性陈述必须引用信源编号**，格式 `[信源N]`，N 是搜索结果列表中的编号
2. **没有信源的判断不要写**，宁可少写不编造。如果某条判断来自你的训练知识但搜索素材中没有，不要写。
3. 某节若搜索素材中完全没有相关信息，写「暂无足够信息」，不要编造。
4. 只列在搜索素材中出现过的上市公司，不凭空添加名称或代码。
5. 不要写市场规模的具体数字，除非搜索素材中明确给出且标注了信源。
6. 不要输出上述 8 节之外的任何内容（不要开场白、不要总结）。
